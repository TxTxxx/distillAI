# 验收记录

## 已完成

- TypeScript 严格检查和 Vite 生产构建。
- 自动化覆盖原生与兼容接口的流式协议、UTF-8/CRLF 分片、错误与提前断流、引用和联网状态。
- 自动化覆盖角色交替、重复启动、暂停边界、晚到响应隔离、问题去重转交、助教快照和中断重试。
- 自动化覆盖 IndexedDB 记录、备份恢复、密钥不持久化、异常备份、资料页码和未读资料隔离。

- 浏览器完成本地模拟的 4 轮讨论、暂停恢复、独立助教追问、手动转交与自动笔记。
- 桌面 1440×1000、平板 820×1180、手机 390×844 和 200% 字号检查；正文与公式无页面水平溢出。
- 真实 PDF 测试文件完成上传、提取文字与原页 Canvas 显示；刷新后会话恢复。
- 音频队列使用虚拟时钟覆盖 20 分钟顺序播放、无重叠与取消后不播放；这不等同于真实语音试听。

- GitHub Actions 首次构建与部署成功（run 34557033639）；`https://txtxx.me/distillAI/` 返回 200，线上 JS/CSS 与本地构建逐字节一致。

- `distill.txtxx.me` 的 CNAME、GitHub Pages 绑定与强制 HTTPS 已完成；在 Chrome 中确认正常打开。
- 新版自主收束测试：至少 3 轮后结束、无效判断继续至上限、评估期间转交的问题不丢失、自定义三角色 Prompt 进入实际请求参数、结束状态恢复。
- Prompt 编辑器在浏览器中完成修改、刷新恢复与还原默认；备份包含提示词和自主结束状态。
- 新版视觉复查 1440×1000、390×844 与 200% 字号：阅读区、播放器与 Agent 入口可用，无页面横向溢出。

## 必须使用真实凭据完成的验收

- 选定的讨论模型：实际网络、CORS、模型权限、工具支持、token 限额与用量。
- World Model、VLA、WAM 三场完整研讨的内容质量，逐条核查关键引用和实验结论。
- 选定语音服务：至少 20 分钟中文夹杂技术术语的连续试听，检查音色稳定、漏段、衔接、倍速和停播恢复。
- 原生搜索返回引用的质量与可访问性；搜索摘要不能替代论文全文核对。
- 生产域名上的真实模型和语音 API 跨域测试。

目前没有使用真实 API 密钥，以上项目不能标为通过。模拟服务与单元测试只证明应用在给定协议下的行为，不证明外部服务当前可用或生成内容正确。

## 2026-09-13 前端重建验证

- 从零替换首页布局与样式，移除旧 studio.css 与 ResearchArt；保留既有接口、调度、音频和会话存储服务。
- 36 项自动测试通过，含 4 项题库测试：全池轮换不重复、方向筛选、小题库与空题库、编辑持久化、畸形数据校验。
- 浏览器实际操作验证：换组、方向筛选、新增自定义问题、移除、撤销后再次移除；问题选择与研究主题输入正常。验收创建的测试问题已清理。
- 桌面 1440、效果图 1505、手机 390、窄窗口 663 宽度截图；手机长文、固定播放器、全屏助教、模型设置；桌面角色 Prompt 编辑器、模型设置均检查。修复窄窗口标题裁切及手机导航辅助标签，补充助教键盘焦点隔离与恢复。
- 设计检测器无报告项。设计对照与独立复核记录保留在本地 .impeccable/review 中。
- 本轮没有模型与 TTS 凭据，未重新运行真实模型/检索调用或连续 20 分钟语音验收，也未完成真实浏览器 200% 文字缩放验收。示例会话仅作 UI 检查。

- 后续独立复核修正：将矩形文字云改为两束由标题边缘扩散的字形；内置题目改名后标题同步。桌面间距使用比例单位，1440px 响应式对照通过（87%），1505px 保持已选构图。手机助教 Escape 关闭与焦点返回已通过浏览器操作检查。
